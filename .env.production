# Production Environment Configuration for Dell PowerEdge R420
# =============================================================
# Hardware: 12-core Xeon, 32GB RAM, 500GB SSD
# OS: Ubuntu 22.04 LTS
# =============================================================

# Environment
ENVIRONMENT=production

# Multiprocessing Configuration
# ✅ PRODUCTION OPTIMIZED: 12-core Dell PowerEdge R420
# With 32GB RAM, we can run more workers with larger models
MP_MAX_WORKERS_CAP=4
MP_CHUNK_DURATION=5.0

# Worker Thread Configuration
# Dual Xeon processors benefit from higher thread counts
TORCH_THREADS=3
OPENCV_THREADS=3

# Memory Management
# With 32GB RAM, we can afford more workers
# Each worker uses ~2-3GB (YOLO + MediaPipe + buffers)
# 11 workers × 2.5GB = ~27GB, leaving 5GB for OS/networking
PRELOAD_MODELS=1

# Gunicorn Configuration (for production deployment)
# Worker count: 2 workers (each handles uvicorn with multiprocessing pool)
# Timeout: 900s (15 minutes) for long video processing
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=900
GUNICORN_WORKER_CLASS=uvicorn.workers.UvicornWorker

# Logging
LOG_LEVEL=INFO
PROD_LOG_LEVEL=INFO

# API Configuration
CVVR_API_ENABLED=1
CVVR_API_TIMEOUT=30

# Performance Tuning
# Sample FPS: 0.5 for speed, increase to 1.0 for better accuracy
SAMPLE_FPS=0.5

# Video Processing
SAVE_ANNOTATED_FRAMES=0
FRAME_SAVE_INTERVAL=1

# Output directory (production path)
OUTPUT_DIR=/opt/poc2/locopilot_evidence
UPLOAD_DIR=/tmp/locopilot_uploads
LOG_DIR=/opt/poc2/logs

# ============================================================================
# MINIO CONFIGURATION (Video Downloads)
# ============================================================================
MINIO_ENDPOINT=mind.snikbtel.uk:9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=login123
MINIO_SECURE=1
MINIO_BUCKET=cvss
